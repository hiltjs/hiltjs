## What problem does this solve

<!-- The problem first, the solution second. If the reasoning only lives here
     and not next to the code, the next reader will not find it. -->

## How

<!-- Anything a reviewer would otherwise have to reconstruct from the diff. -->

## Checklist

- [ ] `pnpm run check` passes
- [ ] Added a changeset (`pnpm changeset`), or this touches nothing published
- [ ] New behaviour is covered by a test that fails without the change
- [ ] If a README sample changed, `examples/` changed with it

<!-- The kernel imports nothing that renders, and everything in it is testable
     without a view. If this pull request needs either, say why here. -->
