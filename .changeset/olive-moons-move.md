---
'@hiltjs/core': patch
---

Point `repository`, `bugs` and `homepage` at the project's own organisation,
`github.com/hiltjs/hiltjs`. GitHub redirects the previous location, but the
manifest should name where the project actually lives.
